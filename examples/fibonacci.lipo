// Fibonacci sequence example
// Calculates and prints the first 20 Fibonacci numbers

ilo fib (n) open
    // Base case: fib(0) = 0, fib(1) = 1
    n lili_sama 1 la open
        pana n
    pini
    // Recursive case
    a jo fib(n - 1)
    b jo fib(n - 2)
    pana a + b
pini

// Print first 20 Fibonacci numbers
i jo 0
wile i lili 21 la open
    result jo fib(i)
    toki("fib({i}) = {result}")
    i jo i + 1
pini
